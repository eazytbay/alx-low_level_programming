#include <stdio.h>
/**
 * main - A program that prints all possible combination single digit number
 * Return: 0 = success
 */
int main(void)
{
int a = 0;
printf("%d", a);
while (++a <= 9)
{
printf(", %d", a);
}
printf("\n");
return (0);
}

