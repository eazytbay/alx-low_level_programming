#include <stdio.h>
/**
 * main -A program that prints all single digit number of base 10
 * starting from zero
 * Return: 0 = success
 */
int main(void)
{
int n = 0;
while (n < 10)
{
putchar(n + '0');
g++;
}
putchar('\n')
return (0);
}
